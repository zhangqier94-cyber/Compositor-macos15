# Compositor 中文体验版 · macOS 15 移植说明

> 目标：让这个原本要求 macOS 26.5 的应用，在你这台 **iMac19,1 / macOS 15.8** 上跑起来。
>
> 结果：**已实测构建成功并正常运行**（见 `运行截图-macOS15.png`，中文界面完整）。
> 全程只用 macOS 自带的命令行工具（Command Line Tools），**不需要 Xcode，不需要 Apple ID**。

---

## 一、为什么先前那条路走不通，现在这条路为什么行

| 路径 | 结论 |
|---|---|
| 直接装官方 DMG | ✗ 二进制 `minos = 26.5`，且 15 个符号在 macOS 15.8 上不存在，启动即崩 |
| 用 Xcode 26.3 重编译 | ✗ 需要 Apple ID，而你的账号被 Apple 拒（「暂时不符合使用此应用程序的条件」） |
| **用系统自带 CLT 编译** | **✓ 本次走通的路** |

关键发现：**macOS 的 Command Line Tools 里已经有 `swiftc`、`clang` 和完整的 macOS SDK**，
而这个工程**零远程依赖**（没有 SPM 包、没有 Sparkle），所以完全不需要 `xcodebuild` 那套构建系统。

本机环境实测：

```
swiftc        6.1.2
可用 SDK      MacOSX15.sdk / 15.2 / 15.4 / 15.5 / 26 / 26.2   ← 15.5 可用
工程规模      115 个 .swift + 8 个 .c，共约 2.4 万行
```

---

## 二、三步操作

```bash
cd "/Users/lee.fu/WorkBuddy/2026-09-23-10-29-48/Compositor-macos15"

./build-clt.sh     # 1. 编译（约 2~3 分钟）
./make-app.sh      # 2. 装配成 .app
open ~/Applications/Compositor.app   # 3. 运行
```

如果 macOS 提示「无法验证开发者」：在 `~/Applications` 里**右键点应用 → 打开 → 再点「打开」**。

`build-clt.sh` 会自动挑一个与 `swiftc` 版本匹配的 SDK（SDK 与编译器版本不匹配会直接报
`this SDK is not supported by the compiler`，本机就踩过这个坑）。

---

## 三、源码改了什么（共 87 个文件，但真正的 API 改动只有 5 处）

### 1) macOS 26 专有 API（5 处，纯外观）

| 文件 | 原代码 | 处理 |
|---|---|---|
| `Compositor/ContentView.swift:160` | `ToolbarSpacer(.fixed, placement: .navigation)` | 换成固定宽度占位工具栏项 |
| `Compositor/ContentView.swift:173` | `ToolbarSpacer(.flexible, placement: .navigation)` | 换成可伸缩占位项 |
| `Compositor/ContentView.swift:169` | `.sharedBackgroundVisibility(.hidden)` | 删除（macOS 15 无等价 API） |
| `Compositor/UI/TypeControls.swift:95` | `button.borderShape = .capsule` | 删除 |
| `Compositor/UI/BlendModePicker.swift:15` | `button.borderShape = .capsule` | 删除 |

`borderShape` 是 AppKit 属性，**已用 `respondsToSelector` 在本机实测确认不存在**。
这两处删除后弹出按钮回落为系统默认边框外观。

### 2) 并发隔离标注（约 80 个文件）

这是本次移植**真正的技术难点**，值得记下来。

原工程的构建设置里有：

```
SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor
SWIFT_APPROACHABLE_CONCURRENCY = YES
```

意思是：**所有没显式标注隔离的声明，默认都在主线程上**。作者据此写了 150 多处 `nonisolated`
来把需要后台执行的东西摘出去。

问题在于：`-default-isolation MainActor` 是 **Swift 6.2 才有的开关**，而本机 CLT 的编译器是
**6.1.2，完全不认这个参数**（我实测过 `-default-isolation` 和 `-Xfrontend -default-isolation`，
都报 `unknown argument`）。

结果就是：默认隔离从「MainActor」变成了「nonisolated」，凡是要在主线程上访问的状态立刻全线报错。

处理办法是**把这个默认设置显式写进源码**，恢复原本的语义：

- 顶层 / 嵌套的 `class`、视图类型、`@Observable` 模型 → 补 `@MainActor`
- 纯数据类型（`struct`/`enum`，如 `ImageLayer`、`ShortcutChord`、`CanvasViewport`）→ 标 `nonisolated`
  （它们只是数据，标了主线程隔离反而会让 `static let` 初始化报错）
- `actor` → 不动（actor 自带隔离，加全局 actor 是编译错误）
- 扩展 → **跟随被扩展类型的隔离**（这条最容易被忽略，一开始漏了导致连锁报错）
- `MetalLayerEffects` → 改为 `nonisolated`（GPU 渲染器，无 UI 状态）

> 顺带一提：`SwiftUI.ToolbarSpacer`、`SwiftUI._TaskValueModifier2` 等 10 个内部符号
> **一行代码都不用改** —— 部署目标降到 15 之后，编译器会自动换成 macOS 15 那套实现。

### 3) 部署目标

`Compositor.xcodeproj/project.pbxproj` 里 4 处 `MACOSX_DEPLOYMENT_TARGET` 由 `26.5` 改为 `15.0`。
（走命令行构建时，部署目标由 `build-clt.sh` 的 `-target x86_64-apple-macos15.0` 决定，
这个改动只在你想用 Xcode 打开工程时才起作用。）

---

## 四、`make-app.sh` 的打包思路

这是个能省掉一大堆麻烦的技巧：**官方 DMG 里的 `.app` 已经包含了编译好的资源**，直接复用：

- `Assets.car`（1.8 MB，图像资源）
- `zh-Hans.lproj/Localizable.strings`（98 KB，730 条词条的完整汉化）
- `AppIcon.icns`、`en.lproj`

所以脚本只做四件事：

1. 拷贝官方 `.app` 的完整包结构
2. **把 `Contents/MacOS/Compositor` 换成我们自己编译的那份**
3. 改写 `Info.plist`：`LSMinimumSystemVersion` 由 `26.5` 改为 `15.0`
4. 重新做本地临时签名（`codesign --sign -`，不需要开发者证书）

这样连 `actool`（编译资源目录的工具，CLT 里没有）都不需要。

---

## 五、验证结果

```
导入符号          2147 个
本机缺失          0 个      ← 官方原版缺失 15 个，必然启动崩溃
架构              x86_64
最低系统          15.0
签名              adhoc
实际运行          成功，中文界面完整（见截图）
```

---

## 六、已知差异与残余风险

**确定的外观差异（不影响功能）**

1. 工具栏里的标签条会带上默认背景色（原版在 macOS 26 上是透明的）—— 因为
   `sharedBackgroundVisibility` 在 macOS 15 上没有替代写法。
2. 字体选择器、混合模式选择器的圆角变成系统默认样式（去掉了 macOS 26 的胶囊形）。

**其他**

- 二进制是 `-Onone`（不优化）编译的，体积 13 MB、启动略慢。想要更快可以改用 `-O` 重编
  （把 `build-clt.sh` 里的 `-Onone` 换成 `-O`，多等几分钟）。
- 工程里 8 个 C 文件（`Compositor/Rendering/*.c`）必须一起编译，且要带
  `-import-objc-header Compositor/Compositor-Bridging-Header.h`，否则会报
  `cannot find 'heal_coverage_bounds' in scope` 之类的错。
- 应用是**本地临时签名**的，只在本机可用；要分发得用开发者证书重签并公证。

---

## 七、排错

| 现象 | 原因与处理 |
|---|---|
| `this SDK is not supported by the compiler` | SDK 与 swiftc 版本不匹配。换 15.x 的 SDK（`build-clt.sh` 会自动挑） |
| `cannot find 'xxx' in scope`（C 函数名） | 少编译了 C 文件或漏了桥接头 |
| `main actor-isolated … in a synchronous nonisolated context` | 某处缺 `@MainActor`，按报错位置补上 |
| `declaration can not have multiple global actor attributes` | `@MainActor` 写重了 |
| 双击提示「无法验证开发者」 | 右键 → 打开 → 再点「打开」 |
| 想跑单元测试 | 本流程不含测试目标（`CompositorTests` 未编译） |

---

## 八、另一条路（如果哪天想用 Xcode）

`build.sh` 是留给 Xcode 路线的脚本。需要 **Xcode 26.0 ~ 26.3**：

- 26.0–26.3：要求 macOS 15.6+，提供 Universal 版本，你的 15.8 刚好够
- 26.4 起：要求 macOS 26.2+，且只提供 Apple Silicon 版本，装了也打不开

但这条路要先解决 Apple ID 被拒的问题，而且下载量 11 GB —— 既然命令行路线已经跑通，
除非要改代码后频繁调试，否则没必要走。

---

## 九、上游更新了，本地怎么跟

### 先明确三件事

**1）这个应用不会自己更新。** 实测本应用**没有链接 Sparkle**，`Info.plist` 里也没有
`SUFeedURL` / `SUEnableAutomaticChecks`（仓库里那个 `appcast.xml` 只是发布用的静态文件，
没有被打进应用）。所以它**不会偷偷升级，也不会因为上游更新而自己坏掉**。

**2）要跟的是汉化 fork，不是原版。** 有两个上游：

| 仓库 | 内容 | 要不要跟 |
|---|---|---|
| `Penny777btc/Compositor` | **汉化版**，发布在 `zh-beta-v*` 标签 | ← 要跟这个 |
| `robbietilton/Compositor` | 英文原版，发布 `v1.2.x` | 仅作参考 |

**3）汉化版目前落后原版。** 当前汉化最新是 `zh-beta-v1.2.2.1`，而英文原版已经到 `v1.2.4`。
也就是说原版的新功能要等汉化作者翻译完才会出现在汉化版里 —— 这是作者的节奏，急不来。

### 怎么跟

```bash
cd "/Users/lee.fu/WorkBuddy/2026-09-23-10-29-48/Compositor-macos15"

./update.sh --check    # 先看一眼有没有新版（不改动任何东西）
./update.sh            # 有新版就一条命令走完
```

`update.sh` 会依次做：

1. 拉取上游全部 `zh-beta-*` 标签，用数值比较挑出最新的那个
2. 顺手提示英文原版到哪个版本了，方便你判断汉化滞后多少
3. 在工作区干净的前提下，把当前分支备份为 `backup/before-<版本>-<时间戳>`
4. 从 GitHub release 下载新版本的 `.dmg` 并校验 `SHA-256`
5. 把本工程**全部移植提交**重放到新标签上（`git rebase --onto`，不是只搬一个提交）
6. 事后修正：白名单扫一遍部署目标、扫一遍 macOS 26 专属 API
7. 编译 → 装配 → 校验，最后告诉你新应用在哪

### 失败了会怎样

**不会把你的成果弄丢。** 三种失败都有兜底：

| 情况 | 脚本行为 |
|---|---|
| 上游改动了我们打过补丁的同一段代码 → 冲突 | 列出冲突文件 → **自动回滚** → 分支精确复位 → 备份分支保留 |
| 新版源码又用了 macOS 26 新 API | 编译失败，提示是哪一类问题；把报错发我，我逐个修 |
| 下载或校验失败 | 直接退出，什么都不改 |

回滚命令（脚本会在失败时提示你）：

```bash
git checkout -B macos15-port <备份分支名>
./build-clt.sh && ./make-app.sh
```

### 仓库长什么样

```
macos15-port 分支
  ├── 608ae5e  zh-beta-v1.2.2.1      ← 移植基线（上游汉化版）
  │     └── 4 个移植提交（源码适配 / 脚本 / 修复）
  ├── patches/macos15-port.patch     ← 全部改动的独立补丁，可单独取用
  ├── build-clt.sh                   ← 编译
  ├── make-app.sh                    ← 装配
  └── update.sh                      ← 跟随更新
```

`build.sh` 是 Xcode 路线的遗留脚本（见第八节），日常用不到。

> **已知的坑（脚本里已处理，但值得知道）**
>
> - 装配时要在 `/Volumes` 的多个候选包之间选资源。本机曾同时挂着英文原版 v1.2.4 和汉化版
>   v1.2.2.1，仅凭卷名 + 字母序会挑中**英文**那个，中文本地化直接丢失。
>   现在改为**校验包内容**（必须含 `zh-Hans.lproj`），并把候选逐个列出来。
> - 全角字符（如「）」）紧贴变量名会被 bash 3.2 吃进变量名，报
>   `xxx: unbound variable`。脚本已全部改用 `${VAR}` 形式。
